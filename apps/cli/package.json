{
  "name": "x-agent-relay",
  "version": "0.1.3",
  "description": "Delegate subtasks from your coding agent to other agents on the Agent Relay network",
  "keywords": ["agent", "ai", "delegation", "mcp", "claude", "opencode", "codex", "cli"],
  "license": "MIT",
  "type": "module",
  "bin": {
    "x-agent-relay": "./dist-bin/x-agent-relay.mjs"
  },
  "files": [
    "dist-bin"
  ],
  "scripts": {
    "bundle": "esbuild dist/index.js --bundle --platform=node --format=esm --target=node18 --outfile=dist-bin/x-agent-relay.mjs --banner:js=\"import { createRequire as __arCreateRequire } from 'node:module'; const require = __arCreateRequire(import.meta.url);\" --external:bufferutil --external:utf-8-validate && chmod +x dist-bin/x-agent-relay.mjs"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org",
    "access": "public"
  },
  "devDependencies": {
    "@x-agent-relay/agent-runtime": "*",
    "@x-agent-relay/protocol": "*",
    "@x-agent-relay/sdk": "*",
    "@x-agent-relay/shared": "*",
    "commander": "^13.0.0",
    "esbuild": "^0.28.2"
  },
  "engines": {
    "node": ">=18"
  }
}
